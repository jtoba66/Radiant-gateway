# ==================== RADIANT GATEWAY CONFIGURATION ====================
# Copy this file to .env and configure your settings
# All settings have sensible defaults - only change if needed

# ==================== SERVER CONFIGURATION ====================
PORT=3001

# ==================== CACHE CONFIGURATION ====================
# Enable/disable local file caching
CACHE_ENABLED=true

# Maximum cache size in GB (LRU eviction when exceeded)
MAX_CACHE_SIZE_GB=50

# ==================== JACKAL NETWORK CONFIGURATION ====================
# gRPC endpoint for querying active storage providers
# Default uses Polkachu's public endpoint
JACKAL_GRPC_ENDPOINT=jackal-grpc.polkachu.com:17590

# ==================== PROVIDER CONFIGURATION ====================
# Timeout for provider requests in milliseconds
PROVIDER_TIMEOUT=5000

# Timeout for Tier 1 providers in milliseconds
TIER1_TIMEOUT=5000

# ==================== LARGE FILE CONFIGURATION ====================
# Files larger than this (in MB) get special handling:
# - Videos: Always stream, bypass Cloudflare cache
# - Non-videos: Force download, bypass Cloudflare cache
LARGE_FILE_THRESHOLD_MB=90

# ==================== PERSISTENCE CONFIGURATION ====================
# How often to save state to disk (in milliseconds)
# Provider health, cache access times, metrics, gRPC cache
SAVE_INTERVAL=300000

# Directory for persistent data files
DATA_DIR=./data

# ==================== GRPC CACHE CONFIGURATION ====================
# How long to cache gRPC provider list (in milliseconds)
# Default: 24 hours (86400000 ms)
GRPC_CACHE_TTL=86400000

# ==================== CLOUDFLARE INTEGRATION ====================
# No configuration needed!
# The gateway automatically sends X-Cloudflare-Bypass headers
# Your Cloudflare Worker reads these headers to make caching decisions

# ==================== NOTES ====================
# - All time values are in milliseconds
# - File paths are relative to project root
# - Boolean values: true/false (case sensitive)
# - Changes require gateway restart to take effect
